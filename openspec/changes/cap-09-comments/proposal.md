# Comments (placeholder)

## Why

Comments were one of nah-vision's January specs, and ADR-0008 cut them from the MVP. On 2026-09-15 they became a change of their own, to be specified later. This change holds no specs yet; its January scope sits beside it in `from-nah-vision.md`.

## What Changes

- Nothing yet. `skip_specs: true` in `.openspec.yaml` keeps `openspec validate --all` passing until comments are specified, and comes off then.

## Capabilities

### New Capabilities

None yet.

### Modified Capabilities

None.

## Impact

None until it is specified. When it is, these already bear on who can see a comment: `product-principles` (no chat, no numbers about people, moments stay in their circle), ADR-0018 (only the poster sees reactions), and cap-02 (nobody can see who is in someone else's circle).
