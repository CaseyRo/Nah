# Archive (placeholder)

## Why

The personal archive, and resurfacing a person's own moment from a year ago, give someone a reason to open Nah? on a day when nobody else posted (CDI-1857, M6). On 2026-09-15 they became a change of their own, to be specified when M6 comes up.

## What Changes

- Nothing yet. `skip_specs: true` in `.openspec.yaml` keeps `openspec validate --all` passing until the archive is specified, and comes off then.

## Capabilities

### New Capabilities

None yet.

### Modified Capabilities

None.

## Impact

None until it is specified. When it is: CDI-1857's per-circle archive was written for shared circles and needs restating under ADR-0017, and cap-03's person page, cap-07's export and leaving, and the places a person adds to moments all bear on it.
